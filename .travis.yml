language: cpp

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - sourceline: 'ppa:george-edison55/precise-backports'
    packages:
      - cmake
  
before_script:
  - mkdir build
  - cd build
  - cmake ..
  
script: make